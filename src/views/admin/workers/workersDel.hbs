<html lang="uz"></html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Foydalanuvchini O'chirish</title>
    <style>
      body { font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #ffe0b2, #ffcc80); display: flex;
      justify-content: center; align-items: center; height: 100vh; margin: 0; }
      form { background: #ffffff; padding: 30px; border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2); width: 100%; max-width: 500px;
      display: flex; flex-direction: column; gap: 15px; } h1 { text-align:
      center; color: #e64a19; font-size: 24px; margin-bottom: 20px; }
      input[type="number"], input[type="email"] { width: 100%; padding: 12px;
      border: 1px solid #ddd; border-radius: 8px; font-size: 16px; }
      input[type="number"]:focus, input[type="email"]:focus { border-color:
      #e64a19; outline: none; box-shadow: 0 0 5px rgba(230, 74, 25, 0.5); }
      button { width: 100%; padding: 12px; background: #e64a19; color: white;
      border: none; border-radius: 8px; font-size: 16px; cursor: pointer;
      transition: background-color 0.3s, transform 0.2s; } button:hover {
      background-color: #d84315; transform: scale(1.02); } button:active {
      transform: scale(0.98); } .form-group { display: flex; flex-direction:
      column; gap: 10px; }
    </style>
  </head>
  <body>
    <form action="/worker/delete" method="POST">
      <h1>Foydalanuvchini O'chirish</h1>
      <div class="form-group">
        <input
          type="number"
          name="worker_id"
          placeholder="Xodim ID"
          required
        />
        
      </div>
      <button type="submit">O'chirish</button>
    </form>
  </body>
</html>